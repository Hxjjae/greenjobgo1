app:
  auth:
    header-scheme-name: authorization
    token-type: Bearer
    access-secret: DeaguGreenArtAcademyClass502ForJavaSpringBoot
    accessTokenExpiry: 3_600_000 # 1??
    refresh-secret: ForJavaSpringBootClass502DaeguGreenArtAcademy
    refreshTokenExpiry: 1_296_000_000 # 15?
    redis-access-black-key: "A-AT-BLACK"
    redis-refresh-key: "A-RT"
  oauth2:
    authorized-redirect-uris:
      - http://localhost:8080/oauth/redirect #?????
      - http://localhost:8080/oauth/redirect

springboot:
  jwt:
    access-secret: DeaguGreenArtAcademyClass502ForJavaSpringBootaaaasdf
    refresh-secret: HelloWorldRefreshTokenCreateBySecretHaHaaasdfasdf
    header-scheme-name: authorization
    token-type: Bearer

spring:
  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 50MB

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/team_green
    username: team_green
    password: green502

  jpa:
    hibernate:
      ddl-auto: update # Entity? ??? ?? ???? ???.
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost:8080
      port: 6379

# swagger
springdoc:
  swagger-ui:
    path: /swagger.html

file:
  dir: /home/download
